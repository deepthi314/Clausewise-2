@echo off
git config user.email "user@clausewise.ai"
git config user.name "ClauseWise User"
git add .
git commit -m "Update: Multilingual features, Chatbot improvements, and Bug fixes"
git push -u origin main
